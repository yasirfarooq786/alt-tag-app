{
  "name": "alt-tag-app",
  "version": "1.0.0",
  "engines": {
    "node": "18.x",
    "npm": "9.x"
  },
  "scripts": {
    "heroku-postbuild": "cd alt-tag-generator && npm install && npm run build"
  },
  "dependencies": {}
}
